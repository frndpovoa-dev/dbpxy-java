_pod_name: localhost

app:
  dbpxy:
    hostname: localhost
    port: 9090
  dbpxy-datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    props:
      user: postgres
      password: postgres

logging:
  level:
    root: info
    com.dbpxy: info
